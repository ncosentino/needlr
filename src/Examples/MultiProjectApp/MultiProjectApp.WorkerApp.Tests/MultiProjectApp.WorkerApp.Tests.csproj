<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <!-- This project intentionally keeps NeedlrAutoGenerate=true (the default) so the test
         assembly itself participates in source generation. -->
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\..\NexusLabs.Needlr.Injection.SourceGen\NexusLabs.Needlr.Injection.SourceGen.csproj" />
    <ProjectReference Include="..\MultiProjectApp.WorkerApp\MultiProjectApp.WorkerApp.csproj" />
  </ItemGroup>

</Project>
