# API Reference (Stable)

<!--
This is a tracked static placeholder that exists so `mkdocs build --strict`
in ci.yml has a valid source file to render for /api/stable/. The real
content is regenerated by `scripts/generate-versioned-api-docs.sh` at
release time and deployed to gh-pages.

Under the new docs pipeline architecture (both workflows `rm -rf` the
slices they don't own before deploy, `peaceiris/actions-gh-pages@v4`'s
`keep_files: true` preserves everything else), this file is NEVER
overwritten by ci.yml-generated content on gh-pages — the real
release-time stable docs live on gh-pages and are preserved forever.

For the live stable API reference, see:
  https://ncosentino.github.io/needlr/api/stable/
-->

The latest stable API reference is published on the generated documentation
site. This placeholder exists only so the mkdocs source build has a valid
file to render.

**Browse the live stable API reference:**
<https://ncosentino.github.io/needlr/api/stable/>

**Other views:**

- <https://ncosentino.github.io/needlr/api/dev/> — current development API (latest main)
- <https://ncosentino.github.io/needlr/api/> — version catalog across all releases
